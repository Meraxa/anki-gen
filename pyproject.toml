[project]
name = "anki-gen"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["genanki>=0.13.1", "pydantic-ai>=1.0.10"]

[dependency-groups]
dev = ["commitizen>=4.9.1", "pre-commit>=4.3.0", "ruff>=0.13.2"]
